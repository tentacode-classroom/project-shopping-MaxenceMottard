{% extends 'base.html.twig' %}

{% block title %}
    Login
{% endblock %}

{% block body %}
    <h1>Connexion</h1>

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form action="{{ path('login') }}" method="post" class="form-signin">
        <img class="mb-4" src="/assets/svg/logo.svg" alt="" width="72" height="72">


        <label for="inputEmail" class="sr-only">Username</label>
        <input type="text" id="username" name="_username" class="form-control" placeholder="Email address" required
               autofocus value="{{ last_username }}">

        <label for="password" class="sr-only">Password</label>
        <input type="password" id="password" name="_password" class="form-control" placeholder="Password" required>

        <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>
    </form>
{% endblock %}
